---
title: Why Move?
description: Learn why we use Move && Sui.
---

# Why Move?

1. Asset Security :: Reduce dynamic calls to avoid security problems caused by heavy ([source(opens in a new tab)](https://docs.sui.io/learn/architecture/consensus))


2. Resource Type Design :: Reduce the mental burden of business design  ([source(opens in a new tab)](https://docs.sui.io/sui-jsonrpc#unsafe_batchTransaction)The underlying interface supports the creation of unsigned batch transactions


3. Engineering Modularity :: Improve business development efficiency and maintainability ([source(opens in a new tab)](https://docs.sui.io/learn/architecture/consensus))

## Why Sui ?

1. Delayed optimization on design trade-offs :: Transactions that do not involve manipulating shared objects do not go through sorting, reducing the time and effort required to process transactions and improving latency ([source(opens in a new tab)](https://docs.sui.io/learn/architecture/consensus))


2. Batch processing :: The underlying interface supports the creation of unsigned batch transaction  ([source(opens in a new tab)](https://docs.sui.io/sui-jsonrpc#unsafe_batchTransaction)The underlying interface supports the creation of unsigned batch transactions


3. Transaction Sponsorship :: Lower the barrier to entry for users to use the network ([source(opens in a new tab)](https://docs.sui.io/learn/architecture/consensus))
